/**
 * Base URL of the .NET API. In dev the API runs on http://localhost:5226
 * (plain HTTP, since we disabled HTTPS redirect in Development).
 */
export const API_BASE_URL = 'http://localhost:5226/api';
